name: modern_form_app
description: A modern Flutter registration app with PHP backend integration.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <4.0.0'  # More compatible SDK requirement
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6  # Older, more compatible version
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0  # Compatible version

flutter:
  uses-material-design: true
